spring:
  application:
    name: jdbcTutorial
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate : true
  datasource:
    url: jdbc:mysql://00000:3306/test?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: toor
    driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 9000